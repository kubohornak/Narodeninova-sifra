const STORAGE_KEY="narodeninova-sifra-progress-v2";const $=id=>document.getElementById(id);const pageTitle=$("pageTitle"),eyebrow=$("eyebrow"),introText=$("introText"),gameArea=$("gameArea"),finalArea=$("finalArea"),puzzleImage=$("puzzleImage"),voucherImage=$("voucherImage"),downloadVoucher=$("downloadVoucher"),answerInput=$("answerInput"),answerLabel=$("answerLabel"),submitButton=$("submitButton"),message=$("message"),progressLabel=$("progressLabel"),progressPercent=$("progressPercent"),progressBar=$("progressBar"),finalTitle=$("finalTitle"),finalText=$("finalText"),resetButton=$("resetButton"),hintButton=$("hintButton"),hintBubble=$("hintBubble");function norm(v){return v.trim().toLocaleLowerCase("sk-SK")}function load(){const n=Number(localStorage.getItem(STORAGE_KEY));return Number.isInteger(n)&&n>=0&&n<=GAME_CONFIG.puzzles.length?n:0}let currentPuzzle=load();function save(){localStorage.setItem(STORAGE_KEY,String(currentPuzzle))}function closeHint(){hintBubble.classList.add("hidden");hintButton.setAttribute("aria-expanded","false")}function render(){pageTitle.textContent=GAME_CONFIG.pageTitle;eyebrow.textContent=GAME_CONFIG.eyebrow;introText.textContent=GAME_CONFIG.introText;finalTitle.textContent=GAME_CONFIG.finalTitle;finalText.textContent=GAME_CONFIG.finalText;voucherImage.src=GAME_CONFIG.voucherImage;downloadVoucher.href=GAME_CONFIG.voucherImage;downloadVoucher.setAttribute("download","narodeninova-poukazka");const total=GAME_CONFIG.puzzles.length;if(currentPuzzle>=total){gameArea.classList.add("hidden");finalArea.classList.remove("hidden");progressLabel.textContent=`Hotovo — ${total} z ${total}`;progressPercent.textContent="100 %";progressBar.style.width="100%";closeHint();return}gameArea.classList.remove("hidden");finalArea.classList.add("hidden");const p=GAME_CONFIG.puzzles[currentPuzzle];puzzleImage.src=p.image;puzzleImage.alt=`Šifra ${currentPuzzle+1}`;answerLabel.textContent=`${currentPuzzle+1}. heslo`;hintBubble.textContent=p.hint||"K tejto šifre zatiaľ nie je nápoveda.";answerInput.value="";const shown=currentPuzzle+1,percent=Math.round(shown/total*100);progressLabel.textContent=`Šifra ${shown} z ${total}`;progressPercent.textContent=`${percent} %`;progressBar.style.width=`${percent}%`;message.textContent="";message.className="message";closeHint()}function checkAnswer(){if(currentPuzzle>=GAME_CONFIG.puzzles.length)return;const entered=norm(answerInput.value),correct=norm(GAME_CONFIG.puzzles[currentPuzzle].password);if(!entered){message.textContent="Najprv napíš odpoveď.";message.className="message error";return}if(entered===correct){message.textContent="Správne. Odomykám ďalšiu šifru…";message.className="message success";submitButton.disabled=true;setTimeout(()=>{currentPuzzle++;save();submitButton.disabled=false;render();window.scrollTo({top:0,behavior:"smooth"});if(currentPuzzle<GAME_CONFIG.puzzles.length)answerInput.focus()},500)}else{message.textContent="To nie je správne heslo. Skús ešte raz.";message.className="message error";answerInput.select()}}hintButton.addEventListener("click",e=>{e.stopPropagation();if(hintBubble.classList.contains("hidden")){hintBubble.classList.remove("hidden");hintButton.setAttribute("aria-expanded","true")}else closeHint()});document.addEventListener("click",e=>{if(!hintBubble.contains(e.target)&&e.target!==hintButton)closeHint()});submitButton.addEventListener("click",checkAnswer);answerInput.addEventListener("keydown",e=>{if(e.key==="Enter")checkAnswer()});resetButton.addEventListener("click",()=>{localStorage.removeItem(STORAGE_KEY);currentPuzzle=0;render();window.scrollTo({top:0,behavior:"smooth"});answerInput.focus()});render();
